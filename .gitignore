# Classes compiladas
out/
*.class

# Fonte LaTeX e rascunho em Markdown do relatório -- ficam só locais;
# no repositório entra apenas o PDF final (relatorio/relatorio-grupo9.pdf)
relatorio/template-latex/
relatorio/relatorio.md

# Backups de coleta de benchmark (dados brutos ficam fora do repo)
resultados/*_backup.csv

# SO
.DS_Store
